<!--削除されたコメントの要素を取得して削除する-->
var commentId = '<%= @comment.id %>';
var element = document.getElementById("comment-" + commentId);
element.parentNode.removeChild(element);

<!--必要に応じて削除完了の通知を表示する-->
alert('コメントが削除されました。');
