<!--フォームの内容をリセット-->
$("textarea").val("");
$("input").val("");
<!--モーダルを閉じる-->
$('#NewBookModal').modal('hide');
<!--登録した本の一覧を更新-->
$('.books').html("<%= escape_javascript(render 'public/books/books', books: @books) %>");

<!--背景色-->
$('#NewBookModal').on('shown.bs.modal', function () {
  $('.modal-backdrop').css({
    'background-color': 'rgba(0,0,0,0.5)'
  });
});